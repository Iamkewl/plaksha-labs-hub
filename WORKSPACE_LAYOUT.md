# Workspace Layout

This workspace uses a split layout:

- $ControlFolderName/ contains agent runtime and orchestration assets
- $ProjectFolder/ contains shareable project/source files

## Sharing Workflow

To share software quickly, package only the $ProjectFolder/ folder.

## Agent Runtime

Launch the full workspace via the generated .code-workspace file so agents can see both folders.
